- [ ] **Make batch apply return cancellation without replacing failures.** Change `applyCompression` in `src/range-compression.ts` to return `Promise<CompressionDetails | undefined>`, return `undefined` when `applyRewrite` reports cancelled navigation, and let model, range-validation, session-change, navigation, and persistence errors reject with the error object that produced the failure.
- [ ] **Export one direct completed-batch operation.** Add `compressCompletedBatch(pi, ctx, input): Promise<CompressionDetails | undefined>` to `src/range-compression.ts`; call `prepareCompression`, `prepareRangeCompression`, `reviewRangeCompression` when `input.review` is true, and `applyCompression` in order; return `undefined` for review or navigation cancellation; and do not catch or translate rejected operations.
- [ ] **Call completed-batch compression directly from `pi-tasks`.** Add `pi-context-compress` to the `pi-tasks` runtime `dependencies` and `bundledDependencies`, import `compressCompletedBatch` from `pi-context-compress/range-compression`, replace the `COMPRESSION_REQUEST` and `COMPRESSION_RESULT` exchange with one awaited call using the existing `ExtensionCommandContext`, handle `undefined` as cancellation, and stop task feeding and rethrow the same error object when the call rejects.
- [ ] **Remove the batch-compression event RPC.** Delete `COMPRESSION_REQUEST`, `COMPRESSION_RESULT`, `CompressionRequestSchema`, `CompressionResultSchema`, their batch transport types, `registerBatchCompressionAdapter`, `registerBatchCompression`, and the batch operation branches from `CompressionOperationCoordinator`; replace `registerCompressionServices(api)` in `src/index.ts` with `registerRangeCompressionService(api)`; and keep the range-compression event service and persisted compression entry schemas.
- [ ] **Update documentation and validate the changed paths.** Remove the batch request/result protocol from `PROTOCOL.md`, document `compressCompletedBatch` with its result and error behavior, update the existing `pi-tasks` compression coverage for success, cancellation, and preservation of the rejected error, run `npm run check`, `npm test`, and `npm run knip` in `pi-context-compress`, and run the existing validation commands in `pi-tasks`.