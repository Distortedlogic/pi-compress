- [ ] **Work unit 1 — Confirm ownership and establish the direct dependency.** Find every producer and consumer of `COMPRESSION_REQUEST`, `COMPRESSION_RESULT`, `registerBatchCompression`, and `registerCompressionServices`; confirm that `pi-tasks` is the only batch-compression caller; make `pi-tasks` declare `pi-context-compress` as a runtime dependency and bundled dependency under Pi package rules; use a workspace, file, Git, or published package reference as appropriate while this package is private; and import only `pi-context-compress/range-compression` so the extension entry point is not registered twice.
- [ ] **Work unit 2 — Make Pi extension message writes awaitable.** In Pi core, change `SendMessageHandler`, `ExtensionAPI.sendMessage`, the extension runtime, the loader delegate, and the `AgentSession` binding from `void` to `Promise<void>`; return the original `sendCustomMessage` promise while retaining extension-error reporting; update existing API declarations, documentation, and tests; and verify that callers which ignore the returned promise remain source-compatible while callers such as compression can await write failures.
- [ ] **Work unit 3 — Add one direct batch-compression API.** Export a stable `compressCompletedBatch(pi, ctx, input): Promise<CompressionDetails | undefined>` function from `src/range-compression.ts`; have it wait for idle state, reject pending-message and invalid-session conditions with their existing clear errors, create the batch rewrite plan, draft the summary, perform optional review, revalidate the source, apply the rewrite, and return `undefined` only for user cancellation; do not catch and translate operational errors inside this function.
- [ ] **Work unit 4 — Make the rewrite path preserve write and validation failures.** Update `applyRewrite` to await each `pi.sendMessage` call before it appends the compression marker, keep the marker as the final write, let model, validation, navigation, and persistence errors propagate unchanged, and make navigation or review cancellation return a normal cancelled result instead of throwing a synthetic `compression_failed` error.
- [ ] **Work unit 5 — Preserve idempotency without the event coordinator.** Keep `operationId` and the persisted `CompressionDetails` marker as the durable identity; before a task batch is compressed, check the active branch for an already-applied marker with the same run, plan, batch, and operation identity; return the stored details for an exact retry; reject conflicting reuse with a clear ordinary error; and rely on the serial `pi-tasks` feed loop for single-operation execution instead of the in-memory prepare/apply request state machine.
- [ ] **Work unit 6 — Replace the `pi-tasks` request/result flow with the direct call.** Import `compressCompletedBatch` from the package subpath, pass the same `ExtensionCommandContext` that is currently placed in the event transport, await the function in the task-feeding control flow, treat an `undefined` result as explicit user cancellation, stop task feeding on an exception, and rethrow the same exception object so Pi receives the original type, message, stack, cause, and provider details; remove request IDs, result listeners, result timeouts, status polling, and generic `Task compression did not apply (compression_failed)` formatting from `pi-tasks`.
- [ ] **Work unit 7 — Verify the direct path before transport removal.** Update only existing test suites in the owning repositories to cover successful compression, exact retry idempotency, operation conflicts, summary-provider failure, oversized input, stale leaf or changed rewrite source, review cancellation, navigation cancellation, and rejected `sendMessage` persistence; add an integration case in the existing `pi-tasks` coverage that proves the original thrown error reaches the task runner unchanged and that task feeding stops without losing the completed task history.
- [ ] **Work unit 8 — Remove the obsolete batch event RPC.** After all batch consumers use the direct API, delete the batch request and result channels, batch transport schemas and types, batch adapter registration, batch branches of `CompressionOperationCoordinator`, batch status and cancellation handling, and unused exports; retain the independent manual range-compression event service only if a confirmed external consumer still needs it; and simplify `registerCompressionServices` and `src/index.ts` to register only services that remain in use.
- [ ] **Work unit 9 — Update the supported contract and package metadata.** Document the direct batch API, its required command context, its `CompressionDetails | undefined` result, its cancellation behavior, and its native error propagation in the existing protocol and package documentation; remove obsolete batch event examples and error-code guidance; verify the `./range-compression` export and production dependency packaging; and pin compatible Pi, `pi-context-compress`, and `pi-tasks` versions for the migration.
- [ ] **Work unit 10 — Run final validation and release in dependency order.** Run the existing typecheck, lint, test, and dead-code checks in Pi core, `pi-context-compress`, and `pi-tasks`; run an interactive end-to-end task batch that compresses and continues feeding; run a forced provider failure and confirm that Pi displays the original error instead of `compression_failed`; verify that cancellation does not report an error and that the original branch remains recoverable; then release the awaitable Pi API first, the direct compression API second, and the migrated `pi-tasks` consumer last before removing compatibility code.